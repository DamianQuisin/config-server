server:
  port: 8010

eureka:
  client:
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

zuul:
 prefix: /api
  
 routes:
   Persona:
     path: /persona/**
     
zuul:
  prefix: /api
    
  routes:
    Address:
      path: /tipoDireccion/**
